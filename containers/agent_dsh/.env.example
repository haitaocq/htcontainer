# 1、dsh 基础配置
DSH_HOME=/home/agents/.dsh
DSH_BIND_HOST=0.0.0.0        # Web 绑定地址，0.0.0.0 支持容器端口映射外部访问
DSH_PORT=3080                # Web UI 监听端口
# 必须设置为浏览器访问 Web UI 所用的地址（IP 或域名），否则从非 localhost 访问时
# /api 会因浏览器信任栅栏返回 HTTP 403（选择工作区/调用 RPC 报
# "transport failure for /api/...: HTTP 403"）。支持逗号或空格分隔多个值。
# 示例：DSH_TRUSTED_HOSTS=192.168.1.50  或  DSH_TRUSTED_HOSTS=myhost.example.com
DSH_TRUSTED_HOSTS=
DSH_TOOLS_MODE=code          # 工具沙箱模式：native | code | both（容器内建议 code）
DSH_WORKSPACE=/home/agents/workspace

# 2、LLM 接入（以下两种方式任选其一）

# 2.1 DeepSeek 原生接入：只需设置 API Key
DEEPSEEK_API_KEY=

# 2.2 自定义 OpenAI 兼容 provider：三者都必须设置才会生效
# DSH_MODEL 支持多个模型 ID，使用逗号分隔
# DSH_MODEL_DEFAULT 指定默认模型（未设置时自动取模型列表第一个）
DSH_PROVIDER_ID=custom-provider
DSH_BASE_URL=
DSH_API_TOKEN=
DSH_MODEL=
DSH_MODEL_DEFAULT=
